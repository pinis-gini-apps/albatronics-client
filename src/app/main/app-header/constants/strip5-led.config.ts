import {MaskConfig} from '../models/mask.config';

export const strip5LedConfig: MaskConfig[] = [
  {
    mask: 16,
    protocol: {
      color: 'success',
      interval: 'hz-05',
    },
  },
];
