import {MaskConfig} from '../models/mask.config';

export const strip4LedConfig: MaskConfig[] = [
  {
    mask: 8,
    protocol: {
      color: 'success',
      interval: 'hz-05',
    },
  },
];
